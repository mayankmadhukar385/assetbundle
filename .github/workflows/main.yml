name: Demo Workflow

on: 
  push:
    branches:
      - 'main'
      - 'feature/**'
jobs:
  demo-jobs:
    name: Demo Job
    runs-on: ubuntu-latest
    steps:
      - name: Message 1
        run: echo "this is message 1"
      - name: Message 2
        run: echo "this is message 2"
